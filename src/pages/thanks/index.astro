---
import Layout from '../../layouts/Layout.astro';
import Container from '@components/common/Container.astro';
import Form from '@components/contact/Form.astro';
---
<Layout>
	<div class="page">
		<div class="hero">
			 <h1>
				<span class="en">THANK YOU</span>
        <span class="jp">お問い合わせ送信完了</span>
			</h1>
		</div>
		<div class="body">
			<Container>
				<div class="wrap">
					<main class="main">
						<div class="content">
              <h2>お問い合わせ<br class="sp" />ありがとうございます</h2>
              <div class="text">
                <p>2営業日以内にご連絡させていただきますので、しばらくお待ちくださいませ。</p>
                <p>万が一返信メールが届かない場合や担当者からの連絡が無い場合は、お手数ですが
                <span><a href="mailto:info@webtech-media.jp">info@webtech-media.jp</a></span>
                に直接ご連絡ください。</p>
              </div>
              <div class="btn-wrap">
                <a href="/" class="btn">HOME</a>
              </div>
            </div>
					</main>
				</div>
			</Container>
		</div>
	</div>
</Layout>

<style lang="scss">

	.hero {
		position: relative;
		padding: 16rem 4rem;
		overflow: hidden;
		background-color: #f1f6ff;

		display: flex;
		align-items: center;
		justify-content: center;
		flex-direction: column;

    @include mixin.sp {
      padding: 6.4rem 1.6rem;
    }

		h1 {
			display: flex;
			align-items: center;
      flex-direction: column;
      text-align: center;

      br.sp {
        display: none;

        @include mixin.sp {
          display: block;
        }
      }

      span {

        &.en {
          font-family: var.$font-en;
          font-size: 1.6rem;
          letter-spacing: .08em;
          color: var.$c-main-v;
          font-weight: 500;

          @include mixin.sp {
            font-size: 1.4rem;
          }
        }

        &.jp {
          font-size: 3.6rem;
          font-weight: bold;
          margin-top: .4rem;

          @include mixin.sp {
            font-size: 2.4rem;
            margin-top: 0;
          }
        }
      }
		}
	}

	.page {
		padding: 8rem 0 0 0;
    background-color: #fff;

    @include mixin.sp {
      padding: 5.6rem 0 0;
    }

	}

	.body {
		padding-top: 12rem;
    padding-bottom: 12rem;

    @include mixin.sp {
      padding: 6.4rem 0;
    }
	}
	
	.wrap {
		display: flex;
		justify-content: space-between;
		gap: 4rem;
	}
	
	.main {
		flex-grow: 1;
    width: 100%;
    max-width: 84rem;
    margin: 0 auto;
	}

  .content {
    text-align: center;
    line-height: 2;

    h2 {
      font-size: 2.8rem;
      font-weight: bold;

      @include mixin.sp {
        font-size: 2.2rem;
        line-height: 1.5;
      }
    }

    br.sp {
      display: none;

      @include mixin.sp {
        display: block;}
    }
   
  }

  .text {
    margin-top: 2.4rem;

    p {
      font-size: 1.6rem;
      line-height: 2.5;

      @include mixin.sp {
        font-size: 1.4rem;
        line-height: 2;
      }

      span {
        display: block;

        a {
          font-size: 2.8rem;
          transition: all .3s;

          @include mixin.sp {
            font-size: 2.4rem;
          }

          @include mixin.hover {
            &:hover {
          color: var.$c-main-v;
            }
          }
        }
      }
    }
  }

  .btn-wrap {
    display: flex;
    justify-content: center;
    margin-top: 4rem;

  }

  .btn {
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var.$font-en;
    width: 24rem;
    height: 5.6rem;
    background-color:var.$c-main-v;
    color: var.$c-white;
    font-size: 1.5rem;
    font-weight: bold;
    transition: all .3s;

    @include mixin.hover {
      &:hover {
        background-color: #334186;
      }
    }
  }
	</style>

