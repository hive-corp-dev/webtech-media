---
import Layout from "@layouts/Layout.astro";
import Container from "@components/common/Container.astro";
import { NOT_FOUND_METADATA } from "@data/metadata";
---

<Layout
  title={NOT_FOUND_METADATA.title}
  description={NOT_FOUND_METADATA.description}
  noindex={true}
>
  <div class="page">
    <div class="hero">
      <h1>
        <span class="en">404 NOT FOUND</span>
        <span class="jp">お探しのページが見つかりません</span>
      </h1>
    </div>
    <div class="body">
      <Container>
        <div class="wrap">
          <main class="main">
            <div class="content">
              <p>
                申し訳ございません。お客様がアクセスしようとしたページが見つかりませんでした。
                <br />
                ページが移動または削除されたか、URLの入力間違いの可能性があります。
                <br />
                正しいURLをご入力いただくか、メニューからご覧になりたい情報をお探しください。
              </p>

              <div class="btn-wrap">
                <a href="/" class="btn">HOME</a>
              </div>
            </div>
          </main>
        </div>
      </Container>
    </div>
  </div>
</Layout>

<style lang="scss">
  .hero {
    position: relative;
    padding: 16rem 4rem;
    overflow: hidden;
    background-color: #f1f6ff;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;

    @include mixin.sp {
      padding: 6.4rem 1.6rem;
    }

    h1 {
      display: flex;
      align-items: center;
      flex-direction: column;

      span {
        &.en {
          font-family: var.$font-en;
          font-size: 1.6rem;
          letter-spacing: 0.08em;
          color: var.$c-main-v;
          font-weight: 500;

          @include mixin.sp {
            font-size: 1.4rem;
          }
        }

        &.jp {
          font-size: 3.6rem;
          font-weight: bold;
          margin-top: 0.4rem;

          @include mixin.sp {
            font-size: 2rem;
            margin-top: 0;
          }
        }
      }
    }
  }

  .page {
    padding: 8rem 0 0 0;
    background-color: #fff;

    @include mixin.sp {
      padding: 5.6rem 0 0;
    }
  }

  .body {
    padding-top: 12rem;
    padding-bottom: 12rem;

    @include mixin.sp {
      padding-top: 6.4rem;
      padding-bottom: 6.4rem;
    }
  }

  .wrap {
    display: flex;
    justify-content: space-between;
    gap: 4rem;
  }

  .main {
    flex-grow: 1;
    width: 100%;
    max-width: 84rem;
    margin: 0 auto;
  }

  .content {
    line-height: 2.25;
    text-align: center;

    @include mixin.sp {
      font-size: 1.4rem;
      text-align: left;
    }

    h2 {
      font-size: 2.4rem;
      font-weight: bold;
      border-bottom: 0.2rem solid var.$c-main-v;
      letter-spacing: 0.08em;

      @include mixin.sp {
        font-size: 2rem;
      }
    }

    ul {
      li {
        padding-left: 1em;
        position: relative;

        &::before {
          content: "";
          width: 0.6rem;
          height: 0.6rem;
          border-radius: 50%;
          background-color: var.$c-main-v;
          position: absolute;
          left: 0;
          top: calc(1em - 0.3rem);
        }

        &:not(:first-child) {
          margin-top: 0.8rem;
        }
      }
    }
  }

  .btn-wrap {
    margin: 4rem auto 0;
    display: flex;
    justify-content: center;
  }

  .btn {
    width: 24rem;
    height: 6rem;
    background-color: var.$c-main-v;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var.$c-white;
    font-weight: bold;
    cursor: pointer;
    font-size: 1.6rem;
    transition: all 0.3s;

    @include mixin.sp {
      width: 22rem;
      height: 4.8rem;
      margin: 0 auto;
      font-size: 1.4rem;
    }

    @include mixin.hover {
      &:hover {
        background-color: var.$c-main;
      }
    }
  }
</style>
