---
const {type} = Astro.props;
---

<div class:list={["container", type]}>
  <slot />
</div>

<style lang="scss">
  .container {
    max-width: 120rem;
    width: 100%;
    padding: 0 3.2rem;
    margin: 0 auto;

    // @include mixin.sp {
    //   padding: 0 2.4rem;
    // }

    &.px-m {
      padding: 0 4rem;
    }
  }
</style>