---
import { Image } from "astro:assets";
const {src, alt, type} = Astro.props;
---

<figure class="figure">
  <Image src={src} alt={alt} class:list={['image', type]} />
  <figcaption class="caption"><slot /></figcaption>
</figure>

<style lang="scss">

  .figure {
    display: flex;
    flex-direction: column;
  }

  .image {
    width: 100%;
    height: auto;

    &.border {
      border: .1rem solid var.$c-gray-l;
    }

    &.shadow {
      box-shadow: 0 0 1.6rem 1rem rgba(0,0,0,0.04);

      @include mixin.sp {
        box-shadow: 0 0 1.2rem .6rem rgba(0,0,0,0.04);
      }
    }
  }

  .caption {
    font-size: 1.3rem;
    text-align: center;
    padding: 0 2.4rem;
    margin-top: .8rem;
    color: #687387;
  }
</style>