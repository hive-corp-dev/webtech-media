---
const { class: className, ...rest } = Astro.props;

// const classMap = {
//   primary: 'primary',
//   secondary: 'secondary',
//   danger: 'danger',
//   success: 'success',
// };

// const btnClass = ['btn', classMap[btnType], className];
---

<!-- <button class:list={['btn', className]} {...rest}>ボタンテキスト</button> -->
<button class:list={['btn', className]} {...rest}>ボタンテキスト</button>

{
// false, null, undefinedを含む場合 
}

<style lang="scss">
  .btn {
    border: 1px solid black;
    padding: 1rem;
  }

  .test {
    box-shadow: inset 0 0 10px black;
  }

  .hey {
    color: red;
  }

  .a{
    background-color: green;
  }
</style>